#include<stdio.h>
#include<math.h>
main()
{
   int x,y,r,z,w;
   double d;
   scanf("%d%d",&z&w);
   d=pow(pow(x-z,2)+pow(y-w,2),0.5);
   if(d==r)
    printf("on the circle");
   else if(d<r)
    printf("inside the circle");
   else
    printf("ouside the circle");

}











